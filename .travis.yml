language: node_js
node_js:
  - "node"

before_script:
  - export DISPLAY=:99.0
  - sh -e /etc/init.d/xvfb start

branches:
  only:
    - master
    - /^greenkeeper/.*$/

before_script:
  - npm install -g @angular/cli@latest

script:
  - ng build
  - ng e2e
